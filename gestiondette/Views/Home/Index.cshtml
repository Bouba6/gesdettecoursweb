@{
    ViewData["Title"] = "Home Page";
}





<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>home</p>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>














@* @model gestiondette.Models.Dette

@{
    Layout = "_layout";
}


<main class="p-4 sm:ml-64 overflow-hidden">
    <div class="flex flex-col justify-center items-center">
        <h1 class="text-3xl font-bold">Ajout Dette</h1>
        <div class="md:w-xgg sm:w-900 mx-auto p-6 border-t-black shadow-md border-2 rounded-md m-6">
            <form asp-action="Create" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <!-- Sélection de Client -->
                <div class="">
                    <label for="Client">Client</label>
                    <select name="ClientId" id="Client"
                        class="p-2 mt-3 border border-gray-300 w-full rounded-md focus:outline-none focus:border-blue-500">
                        <option value="">Select a Client</option>
                        @foreach (var client in ViewBag.Clients)
                        {
                            <option value="@client.Id">@client.Surnom</option>
                        }
                    </select>
                </div>

                <!-- Sélection d'Article -->
                <div class="flex flex-row justify-between mt-5 mb-5 ">
                    <div class="w-auto">
                        <label for="Article">Article</label>
                        <select name="ArticleId" id="Article"
                            class="p-2 border border-gray-300 w-auto rounded-md focus:outline-none focus:border-blue-500">
                            <option value="">Select an Article</option>
                            @foreach (var article in ViewBag.Articles)
                            {
                                <option value="@article.Id">@article.Libelle</option>
                            }
                        </select>
                    </div>

                    <!-- Saisie de Quantité -->
                    <div>
                        <label>Quantité:</label>
                        <input type="number" id="qte" class="p-2 border rounded-md" min="1" />
                    </div>
                </div>

                <!-- Bouton pour ajouter un détail -->
                <div class="flex justify-end space-x-2">
                    <button type="button" onclick="addDetail()" class="bg-blue-500 text-white px-4 py-2 rounded">
                        Ajouter
                    </button>
                </div>

                <!-- Tableau des détails temporaires -->
                <table class="w-full mt-4 text-sm text-left text-gray-500">
                    <thead class="text-xs text-gray-700 uppercase bg-gray-900">
                        <tr>
                            <th scope="col" class="px-6 py-5 text-white">Article</th>
                            <th scope="col" class="px-6 py-5 text-white">Quantité</th>
                            <th scope="col" class="px-6 py-5 text-white">Montant</th>
                        </tr>
                    </thead>
                    <tbody id="detailsTable">
                        <!-- Détails ajoutés dynamiquement ici -->
                    </tbody>
                </table>

                <!-- Bouton pour enregistrer la dette -->
                <div class="flex justify-end mt-4">
                    <input type="submit" value="Créer"
                        class="bg-blue-500 hover:bg-blue-700 text-white px-4 py-2 rounded" />
                </div>
            </form>
        </div>
    </div>
</main> *@